#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "circ_buf.h"

int main(int argc, char const *argv[])
{
  // rbuf_t buffer;
  // MessageFrame f;
  // f.last_time_sent = 115;
  // f.times_sent = 1;

  // ringbuf_init(&buffer);

  // // for (int i = 0; i < RBUF_SIZE; ++i)
  // // {
  // //   MessageFrame f;
  // //   f.last_time_sent = i;
  // //   
  // // }
  // ringbuf_put(&buffer, f);
  // MessageFrame k;
  // k.last_time_sent = 12;
  // ringbuf_put(&buffer, k);

  // MessageFrame d = ringbuf_get(&buffer);
  // printf("%d\n", d.last_time_sent);
  // // we can reset buffer counts or clear the contents completely
  // ringbuf_flush(&buffer, RBUF_CLEAR);

  return 0;
}